# Contracts Package

This package holds shared API contracts and transport models for Pool.

The first implementation pass lives under `src/` and currently covers:

- project summary DTOs
- repo DTOs
- ticket detail DTOs
- event DTOs

The next pass should add:

- create/update payload validation
- board/ticket-detail aggregate contracts
- orchestration command payloads
